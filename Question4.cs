//store the radius of a circle in a variable and calculate the area.

using System;
public class Question4
{
    public void Calculatearea()
    {
        int length = 20;
        int breath =30;
        int radius =5;
        int area = (length *breath);
        Console.WriteLine ($"radius of a circle is {radius}" );
        Console.WriteLine();
        Console.WriteLine ($" the total area from the calculation is {area}" );


    }
}